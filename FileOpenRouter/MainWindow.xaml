<Window x:Class="FileOpenRouter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:FileOpenRouter"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Height="700"
        Width="1040"
        FontSize="14"
        FontFamily="Microsoft YaHei UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="TransparentBrush" Color="Transparent"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#E8FFFFFF"/>
        <SolidColorBrush x:Key="PrimaryButtonTextBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6FED"/>
        <SolidColorBrush x:Key="AccentSoftBrush" Color="#E8F1FF"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#D92D20"/>
        <SolidColorBrush x:Key="DangerSoftBrush" Color="#FFF5F5"/>
        <SolidColorBrush x:Key="DangerBorderBrush" Color="#FECACA"/>
        <SolidColorBrush x:Key="TextBrush" Color="#182033"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#566274"/>
        <SolidColorBrush x:Key="ControlBorderBrush" Color="#D7DEE8"/>
        <local:RoundedClipConverter x:Key="RoundedClipConverter" />
        <Geometry x:Key="InfoGeometry">M505.6512 39.0144c-261.2224 3.4816-470.1184 218.112-466.6368 479.4368 3.4816 261.12 218.112 470.1184 479.3344 466.6368 261.2224-3.4816 470.1184-218.112 466.7392-479.3344C981.504 244.4288 766.8736 35.5328 505.6512 39.0144zM558.08 196.608c48.128 0 62.2592 27.9552 62.2592 59.8016 0 39.8336-31.9488 76.6976-86.3232 76.6976-45.568 0-67.1744-22.9376-65.9456-60.8256C468.0704 240.4352 494.7968 196.608 558.08 196.608zM434.7904 807.6288c-32.8704 0-56.9344-19.968-33.8944-107.6224l37.6832-155.5456c6.5536-24.8832 7.68-34.9184 0-34.9184-9.8304 0-52.5312 17.2032-77.7216 34.2016l-16.384-26.9312c79.9744-66.7648 171.8272-105.8816 211.2512-105.8816 32.8704 0 38.2976 38.912 21.9136 98.6112l-43.2128 163.5328c-7.68 28.8768-4.4032 38.912 3.2768 38.912 9.9328 0 42.1888-11.9808 73.9328-36.9664l18.6368 24.8832C552.5504 777.728 467.6608 807.6288 434.7904 807.6288z</Geometry>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style TargetType="Button">
            <Style.Resources>
                <CornerRadius x:Key="ButtonCornerRadius">6</CornerRadius>
                <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="#E8FFFFFF" />
                <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#2F6FED" />
                <SolidColorBrush x:Key="ButtonPressedBackgroundBrush" Color="#DCEBFF" />
            </Style.Resources>
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{DynamicResource ButtonCornerRadius}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ButtonHoverBackgroundBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource ButtonHoverBorderBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource ButtonPressedBackgroundBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Style.Resources>
                <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="#275FD0" />
                <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#275FD0" />
                <SolidColorBrush x:Key="ButtonPressedBackgroundBrush" Color="#1F4FB5" />
            </Style.Resources>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryButtonTextBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Style.Resources>
                <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="#FFF5F5" />
                <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="#D92D20" />
                <SolidColorBrush x:Key="ButtonPressedBackgroundBrush" Color="#FECACA" />
            </Style.Resources>
            <Setter Property="Background" Value="{DynamicResource DangerSoftBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource DangerBorderBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource DangerBrush}" />
        </Style>

        <Style x:Key="InfoButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Style.Resources>
                <CornerRadius x:Key="ButtonCornerRadius">9</CornerRadius>
                <SolidColorBrush x:Key="ButtonHoverBackgroundBrush" Color="#E8F1FF" />
                <SolidColorBrush x:Key="ButtonHoverBorderBrush" Color="Transparent" />
                <SolidColorBrush x:Key="ButtonPressedBackgroundBrush" Color="#DCEBFF" />
            </Style.Resources>
            <Setter Property="MinHeight" Value="18" />
            <Setter Property="Width" Value="18" />
            <Setter Property="Height" Value="18" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource TransparentBrush}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="3" />
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="9,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
        </Style>

        <Style x:Key="GridEditingTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="MinHeight" Value="20" />
            <Setter Property="Margin" Value="0,2" />
            <Setter Property="Padding" Value="3,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="GridCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="{DynamicResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RowHeight" Value="34" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGrid">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="DG_ScrollViewer"
                                          Focusable="False">
                                <ScrollViewer.Clip>
                                    <MultiBinding Converter="{StaticResource RoundedClipConverter}"
                                                  ConverterParameter="7">
                                        <Binding ElementName="DG_ScrollViewer"
                                                 Path="ActualWidth" />
                                        <Binding ElementName="DG_ScrollViewer"
                                                 Path="ActualHeight" />
                                    </MultiBinding>
                                </ScrollViewer.Clip>
                                <ScrollViewer.Template>
                                    <ControlTemplate TargetType="ScrollViewer">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="*" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <DataGridColumnHeadersPresenter x:Name="PART_ColumnHeadersPresenter"
                                                                            Grid.Column="0" />
                                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                                    Grid.Row="1"
                                                                    Grid.Column="0"
                                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                                    Content="{TemplateBinding Content}"
                                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                                    ContentStringFormat="{TemplateBinding ContentStringFormat}" />
                                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                                       Grid.Row="1"
                                                       Grid.Column="1"
                                                       Maximum="{TemplateBinding ScrollableHeight}"
                                                       Orientation="Vertical"
                                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                                       Value="{Binding VerticalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" />
                                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                                       Grid.Row="2"
                                                       Grid.Column="0"
                                                       Maximum="{TemplateBinding ScrollableWidth}"
                                                       Orientation="Horizontal"
                                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                                       Value="{Binding HorizontalOffset, RelativeSource={RelativeSource TemplatedParent}, Mode=OneWay}"
                                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" />
                                        </Grid>
                                    </ControlTemplate>
                                </ScrollViewer.Template>
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Background" Value="{DynamicResource TransparentBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="8,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10,2,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource PanelBrush}"
                BorderBrush="{DynamicResource ControlBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="8,8,8,2"
                Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal"
                            Margin="0,0,0,8">
                    <TextBlock FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"
                               Text="文件打开方式路由器" />
                    <Button Margin="8,1,0,0"
                            Style="{StaticResource InfoButtonStyle}"
                            Click="InfoButton_Click">
                        <Path Data="{StaticResource InfoGeometry}"
                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              Stretch="Uniform" />
                    </Button>
                </StackPanel>
                <TextBlock x:Name="InfoTextBlock"
                           TextWrapping="Wrap"
                           Visibility="Collapsed"
                           Foreground="{DynamicResource MutedTextBrush}"
                           LineHeight="23">
                    <Run Text="1. 本程序不是解压软件、阅读器或编辑器，而是“打开方式路由器”，它只负责判断文件所在目录，并调用你配置的外部程序。" />
                    <LineBreak />
                    <Run Text="2. 请先选择一个真实存在的兜底程序。当前文件未命中任何规则时，本程序会使用该兜底程序打开文件；按住【Shift键】打开文件时，也会强制使用兜底程序。" />
                    <LineBreak />
                    <Run Text="3. 请将本程序设置为对应文件类型的默认打开程序。设置完成后，双击打开这些文件时，本程序会自动根据文件所在目录，将其转交给指定的程序打开。目录越具体则优先级越高。" />
                    <LineBreak />
                    <Run Text="4. 可以复制多份本程序并重新命名，例如 FileOpenRouter_压缩包.exe、FileOpenRouter_pdf.exe，不同名称的程序会使用不同配置文件，适合分别管理 压缩包、PDF 等不同文件类型。" />
                    <LineBreak />
                    <Run Foreground="{DynamicResource DangerBrush}" Text="5. 注意：兜底程序不要选择本程序自身，否则可能造成循环调用。"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="2,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       FontWeight="SemiBold"
                       Margin="0,0,4,0"
                       Text="兜底程序：" />
            <TextBox x:Name="FallbackProgramTextBox"
                     Grid.Column="1"
                     VerticalContentAlignment="Center" />
            <Button Grid.Column="2"
                    MinWidth="100"
                    Margin="10,0,0,0"
                    Content="选择程序"
                    Click="ChooseFallbackProgram_Click" />
        </Grid>

        <DataGrid x:Name="RulesGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="启用"
                                        Width="48"
                                        CanUserResize="False"
                                        ElementStyle="{StaticResource GridCheckBoxStyle}"
                                        EditingElementStyle="{StaticResource GridCheckBoxStyle}"
                                        Binding="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="规则名称"
                                    Width="150"
                                    ElementStyle="{StaticResource GridTextBlockStyle}"
                                    EditingElementStyle="{StaticResource GridEditingTextBoxStyle}"
                                    Binding="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="文件夹路径"
                                    Width="*"
                                    ElementStyle="{StaticResource GridTextBlockStyle}"
                                    EditingElementStyle="{StaticResource GridEditingTextBoxStyle}"
                                    Binding="{Binding Folder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="打开程序路径"
                                    Width="*"
                                    ElementStyle="{StaticResource GridTextBlockStyle}"
                                    EditingElementStyle="{StaticResource GridEditingTextBoxStyle}"
                                    Binding="{Binding Program, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </DataGrid.Columns>
        </DataGrid>

        <WrapPanel Grid.Row="3"
                   HorizontalAlignment="Right">
            <Button MinWidth="100"
                    Margin="0,0,10,0"
                    Content="新增规则"
                    Click="AddRule_Click" />
            <Button MinWidth="100"
                    Margin="0,0,30,0"
                    Style="{StaticResource DangerButtonStyle}"
                    Content="删除选中规则"
                    Click="DeleteSelectedRule_Click" />
            <Button MinWidth="100"
                    Margin="0,0,10,0"
                    Content="选择文件夹"
                    Click="ChooseRuleFolder_Click" />
            <Button MinWidth="100"
                    Margin="0,0,30,0"
                    Content="选择程序"
                    Click="ChooseRuleProgram_Click" />
            <Button MinWidth="100"
                    Margin="0"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Content="保存配置"
                    Click="SaveConfig_Click" />
        </WrapPanel>
    </Grid>
</Window>
